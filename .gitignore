# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example

# Prisma - local SQLite database files (schema/migrations ARE committed)
/prisma/dev.db
/prisma/dev.db-journal

# Media module - local-disk storage adapter output (not committed; swap
# for real cloud storage before production, see server/utils/storage.ts)
/public/uploads

# Internal working docs - kept locally for the dev's own reference only,
# not committed until explicitly decided otherwise
/DIARY.md
/PROJECTDOC.md
